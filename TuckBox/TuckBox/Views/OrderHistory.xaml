<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TuckBox.Views.OrderHistory"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Order History">

    <CollectionView x:Name="OrdersList">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="10" Padding="10">
                    <VerticalStackLayout>
                        <Label Text="{Binding Order_Date}" FontAttributes="Bold" />
                        <Label Text="{Binding City_Name}" />
                        <Label Text="{Binding Time_Slot}" />
                        <Label Text="{Binding Total_Price, StringFormat='Total: {0:C}'}" />
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
